# T1770 Implementation and Review Requirement Matrix

## Authority and evidence snapshot

- Ticket: T1770 — Tight Beam support as an additional Surf Ace provider.
- Canonical requirement authority: `<spec-root>/surf-ace/specs/tight-beam-provider-support.html`.
- Canonical Revision 11 + DEC-TA-01A + FLYNN-1770-02 + CLOSE-12/AC-CLOSE-07 reconciliation SHA-256: `4bbc3b076d3be328ab5f354ad80c3e44ddc338545c600838260abadb3d839741`. Approved-predecessor snapshot before the CLOSE-12 reconciliation: `97a49241d4991a7e96bfe89bff720cbe2acb0742c9781d0d0bb697c2319ecafb`.
- Independent approvals include `scratch/tight-beam-provider-support-codex-review-rev11-flynn-1770-02-delta.md` (SHA-256 `11c79d10a3d772258c1047378f636a97dddc427a155b0435587340af28488a6a`) for the general standalone CLI correction.
- Accepted architecture bounceback: `scratch/T1770-whole-ticket-bounceback-report.md`, SHA-256 `007f741cddc6a74fda0826481e8b2a143ed6f013123bdf7c2ef23eaacae1b364`.
- Parent recon: `<spec-root>/surf-ace-compositor/reports/recon/T1769-surface-provider-decoupling-artifact-recon.html`; Revision 10 is authoritative wherever it differs from A1..A4 or the historical June namespace proposal.
- Worktree base: `4a8294446ee8fbfd56ec4cbe7c68218b43fb8836`.
- Janus REST item state at ingestion: `In Progress`.
- Complete Janus migration-audit snapshot used by the Revision 11 reviewer: 36/36 retained rows, `has_more=false`, including accepted bounceback `PP006813` and resume proof `PP006814`.

This document is an implementation and review checklist derived from the canonical specification. It does not amend or replace that specification.

## Current gate snapshot

- The verified T1770 Revision 11 source is acceptance input to T1778. T1778 expands production admission to the native iPhone and iPad client; the native candidate now implements the complete shared authority and passes the iPhone/iPad source gates, but it does not inherit Electron review, deployment, or product proof.
- The independently approved T1778 Apple amendment is `<spec-root>/surf-ace/specs/native-apple-lockless-client-authority.md`, SHA-256 `04b9e412f23f84a33b54825b8d09ac1763879a552e671d3fd50c72465fe5cc22`. The R1-R11 rollout authority, including the Gate 6 explicit-preparation repair pending independent source-document rereview, is `<spec-root>/surf-ace/specs/lockless-multi-controller-production-rollout.md`; its review-input hash belongs in the external review artifact rather than this self-referential projection.
- The production OpenClaw path has a correlated accepted mutation, exact client commit receipt, visible Markdown render, current local read, and accessibility capture. The supplied context produced the specified `Unknown chat — Clawline` fallback, not the exact `OpenClaw — Clawline` example.
- The earlier dedicated-archetype/MCP Tight Beam proof is rejected architecture evidence only. It does not block the corrected CLI route and supports no product claim.
- Pending rows remain pending until their complete named proof exists. Green omnibus/unit coverage and diagnostic harness evidence do not promote official-path or `Both` rows by themselves.
- The temporary tablet-a proof remains acceptance evidence only and is not T1778 deployment or fleet-soak evidence. T1778 R7/R8 actions and evidence must come exclusively from the shipped Rust CLI; the resident OpenClaw extension stays connected but passive.

## Admitted-platform decision

The T1778 production slice admits Electron and the native `SurfAce` target on iPhone and iPad. Each must implement and prove every applicable requirement below without a semantic carve-out. The exact native candidate advertises the capability only from the complete iOS target and keeps `SurfAceSpatial` non-capable; independent combined review, canonical-main promotion, physical-device deployment, and CLI-only product proof remain separate gates.

`SurfAceSpatial` remains legacy/non-admitted in T1778 and must not advertise the lockless capability. There are no Apple-exclusive T1770 acceptance IDs; `APPLE-AC-01..10` below are an implementation/proof crosswalk, not replacement semantics or added wire requirements. `COMPAT-5` and `AC-MIG-03` activate the complete applicable T1770 check set separately for the iPhone and iPad production build. A platform may omit controller window lifecycle only where no such product operation exists; local-user recoverable close remains unconditional.

## Proof classes

- **Source/unit:** source inspection, deterministic unit/integration tests, or conformance review is the direct proof. These checks do not establish broader product claims.
- **Official path:** exercise the official OpenClaw and Tight Beam controller surfaces against a production Surf Ace client build. Direct protocol scripts, fake clients, mocks, fixtures, compositor-only calls, and uncorrelated logs are diagnostic only.
- **Both:** deterministic source/unit proof is required for concurrency, accounting, ordering, or atomicity, and the corresponding product behavior must also be correlated through the official path.

All statuses begin as `Pending`. A row may move only when its named proof exists. The dated official-path ledger is `docs/design/t1770-official-path-proof-2026-07-30.md`.

## Shared protocol and client-authority core

| AC ID | Primary lane | Required implementation and proof | Proof class | Status |
|---|---|---|---|---|
| AC-ID-02 | Shared protocol/core | Reject a duplicate live controller instance ID with the distinct duplicate error; preserve the incumbent connection and cursors; permit retry only after incumbent reaping. | Both | Pending |
| AC-ID-03 | Shared protocol/core | Admit duplicate human labels under distinct IDs and prove labels have no dedupe, routing, cursor, permission, ordering, or quota effect. | Official path | Pending |
| AC-ARCH-01 | Shared protocol/core | Kill, restart, and partition one controller while the other continues content and topology work against actionable client truth, without election or ownership transfer. | Official path | Pending |
| AC-SYNC-01 | Shared protocol/core | Refuse disconnected content and topology writes: nothing may be accepted, reserved, queued, merged, or replayed; reconnection requires a fresh read and new requests. | Both | Pending |
| AC-SYNC-02 | Shared protocol/core | Prove one authoritative client replica per surface, with no replicated writable surface or optimistic central copy. | Source/unit | Pending |
| AC-SYNC-03 | Shared protocol/core | Enforce the conformance tripwire: offline writes, multi-authoritative replication, or optimistic central state require a product architecture revisit before implementation. | Source/unit | Pending |
| AC-HIST-01 | Shared protocol/core | Interleave A1, B1, A2; allocate distinct ordered revisions and entry IDs; make every push visible; prove Back/Forward order and no same-controller replacement. | Both | Pending |
| AC-HIST-02 | Shared protocol/core | Keep the visible entry outside the 20-entry retained pool; update recency on navigation; evict the least-recently-visible non-visible entry across controllers. | Both | Pending |
| AC-HIST-03 | Shared protocol/core | After Back, remove Forward entries before LRU enforcement when either controller pushes, with controller-independent results. | Both | Pending |
| AC-HIST-04 | Shared protocol/core | Back/Forward must restore exact content, annotation restore state, revision, and entry-bound composite provenance without changing controller cursors. | Official path | Pending |
| AC-HIST-05 | Shared protocol/core | Prove a chatty controller can evict another controller's older entries through ordinary LRU, with diagnostics and no quota, pin, priority, or owner protection. | Both | Pending |
| AC-TOPO-01 | Shared protocol/core | Serialize same-revision competing mutations: commit exactly one; return `stale_topology` plus authoritative revision/tree to the loser; create no partial state or events. | Both | Pending |
| AC-TOPO-02 | Shared protocol/core | Require the loser to refresh, recompute intent, and submit a new request ID at the current expected revision before it can commit. | Both | Pending |
| AC-TOPO-03 | Shared protocol/core | Prove clients and controllers never rebase, merge, transform, partially apply, or silently retry stale topology payloads. | Source/unit | Pending |
| AC-TOPO-04 | Shared protocol/core | Route split, rename, resize, close, restore, and retained realization through stable IDs and the same expected-revision seam. | Both | Pending |
| AC-TOPO-05 | Shared protocol/core | Prove commit order and client allocation follow serialized client receipt, not controller identity or labels; reverse receipt order to reverse outcomes. | Both | Pending |
| AC-TOPO-06 | Shared protocol/core | Return the original response for identical request replay; reject changed-payload ID reuse; require a new ID for recomputed stale intent. | Both | Pending |
| AC-CAP-01 | Shared protocol/core | Prove `P` as the pane-creating admission cap, atomic identity-blind `pane_capacity`, no allocations/events/revision on refusal, close `T`, split back to `P`, capacity-free same-ID restores to `P + T`, `L + R` conservation, later creation refusal, and reversed-identity equivalence. | Both | Pending |
| AC-CAP-02 | Shared protocol/core | Enforce exact annotation, pane, and surface byte limits with distinct atomic `pane_state_capacity` and `surface_state_capacity`; never truncate, evict, advance cursors, or emit events; close/restore exact-at-limit state byte-identically. | Both | Pending |
| AC-CAP-03 | Shared protocol/core | Independently compute and prove the complete `SURF-12` envelope for `P + T` records, consumable scopes, and `C` cursor/gap bundles; keep close lossless and size-safe; accept valid over-`P` state; reject only actual bound violations; restore exact state after restart. | Both | Pending |
| AC-CLOSE-01 | Shared protocol/core | Atomically replace a live pane with a durable tombstone preserving visible content, mixed-controller history/provenance, annotations, unread frames, and cursors without discard or advance. | Both | Pending |
| AC-CLOSE-02 | Shared protocol/core | Let the non-closing controller restore the same pane ID and exact state/cursors; remove the tombstone atomically; repeat at and above `P` without any capacity error. | Both | Pending |
| AC-CLOSE-03 | Shared protocol/core | Reject stale-revision and invalid-placement restores without consuming or changing the tombstone; succeed after refresh, recomputation, and retry. | Both | Pending |
| AC-CLOSE-04 | Shared protocol/core | When a former pane label has been reused, restore the same pane identity/state with a newly allocated unique visible label. | Both | Pending |
| AC-CLOSE-05 | Shared protocol/core | Reject last-live-pane close identically for every controller without creating a tombstone or revision. | Both | Pending |
| AC-CLOSE-06 | Shared protocol/core | With small count and byte bounds, reclaim ascending `closedSequence` tombstones until both bounds hold, independent of controller and provenance. | Both | Pending |
| AC-CLOSE-07 | Shared protocol/core | Prove every admitted controller/user close is already bounded by SURF-12 and never returns `tombstone_capacity`; reject exact retained-tombstone aggregate bound+1 during admission/migration/restart/configuration without source/prior-generation mutation, trim, reclamation, or drop, and accept exact equality. | Both | Pending |
| AC-CLOSE-08 | Shared protocol/core | Ensure list, read, and attempted restore do not refresh reclamation order; after reclamation, reject restore without topology change. | Both | Pending |
| AC-CLOSE-09 | Shared protocol/core | Fan out every reclamation in order and persist tombstone/pane IDs, sequence, size, bounds, and reason; never use owner identity as a selection factor. | Both | Pending |
| AC-SURF-01 | Shared protocol/core | Serialize controller and local-user surface open at the endpoint; allocate client IDs/labels; fan out appeared events; grant no later authority. Without controller lifecycle capability, return `unsupported_operation` while keeping local-user close recoverable. | Both | Pending |
| AC-SURF-02 | Shared protocol/core | Commit a complete surface tombstone before removed event/socket closure and restore exact surface/pane identities, topology, content/history/provenance, annotations, nested tombstones, and unread state through fresh sockets from the other controller. | Both | Pending |
| AC-SURF-03 | Shared protocol/core | Support local-user close of the last surface, zero-live endpoint discovery, and controller/user restore even without controller lifecycle capability; close cause must affect explanation only. | Official path | Pending |
| AC-SURF-04 | Shared protocol/core | Serialize concurrent open, close, and restore; return authoritative state to stale losers with no partial tombstone/event; require fresh recomputation and prohibit automatic retry/merge. | Both | Pending |
| AC-SURF-05 | Shared protocol/core | Count nested state once during bounded surface close; create no new loss or cursor movement; reclaim globally oldest pane/surface tombstones; keep close size-safe; report nested/unread disposition; prove identity reversal yields the same victim. | Both | Pending |
| AC-SURF-06 | Shared protocol/core | Restart with zero live surfaces and a valid over-`P` retained surface; restore endpoint capability, sequences, bounds, exact distribution, scopes, cursors, and gaps before admission; restore without clamp/capacity refusal; keep pane creation refused until prospective count fits. | Both | Pending |
| AC-RET-01 | Shared protocol/core | Preserve dormant-bundle order across reads and label changes; resume exact cursors for a retained ID; allocate a new `dormantSequence` after a later disconnect; start a new ID at current tail. | Both | Pending |
| AC-RET-02 | Shared protocol/core | Enforce total, dormant-count, and dormant-byte bounds using ascending `dormantSequence`; recompute exact shared charges; emit the required event/diagnostic; prove victim choice ignores identity, labels, product, work, and unread volume. | Both | Pending |
| AC-RET-03 | Shared protocol/core | Atomically reclaim a dormant registry entry, all its live/pane-tombstone/surface-tombstone cursors, and frames retained solely for it; report exact unread disposition; preserve other cursors/frames; re-admit at current tail. | Both | Pending |
| AC-RET-04 | Shared protocol/core | Return `controller_capacity` when every admitted entry is live; once one is dormant, reclaim only the oldest eligible bundle; prove restart preserves bounds, state, sequences, and victim. | Both | Pending |
| AC-LIVEBUF-01 | Shared protocol/core | Bound each scope for a connected never-reader while the producer continues; drop oldest complete records by sequence; move only affected cursor floors; create one sticky/coalesced gap, targeted overflow event, matching structured loss, and exact durable diagnostics. | Both | Pending |
| AC-LIVEBUF-02 | Shared protocol/core | Coalesce only declared latest-wins/live state by key; never merge/truncate append or finalized records; convert oversized records to explicit gaps; keep pane/surface close control outside payload pressure and preserve retained range/gap on restore. | Both | Pending |
| AC-READ-02 | Shared protocol/core | Allow mutations and visible lag while another controller is slow/disconnected; catch up within the advertised window; use structured live-buffer loss after crossing it rather than claiming unlimited catch-up. | Official path | Pending |
| AC-OPS-02 | Shared protocol/core | Correlate every accepted mutation, overflow, and reclamation to exactly one client commit and ordered fan-out; expose stable, distinguishable capacity and failure codes. | Both | Pending |

## Electron client and product UI

Electron also owns the client realization of every shared-core row above.

| AC ID | Primary lane | Required implementation and proof | Proof class | Status |
|---|---|---|---|---|
| AC-PROV-01 | Electron client | Render the visible provenance pill exactly as `OpenClaw — Clawline` for the specified friendly chat and product labels. | Official path | Pending |
| AC-PROV-02 | Electron client | Bind composite provenance to each history entry so Back/Forward shows captured entry metadata rather than current connection/chat metadata. | Official path | OpenClaw production path passed; Tightbeam path pending |
| AC-PROV-03 | Electron client | Implement deterministic localized two-part fallbacks for chat-only, provider-only, neither, empty, and whitespace-only inputs in two locales while preserving supplied labels verbatim. | Official path | Pending |
| AC-PROV-04 | Electron client | At every supported accessibility size, prove the composite, collapsed-ellipsis, and zero-width classes for long LTR, RTL, and mixed labels; preserve navigation and full accessible text in every class. | Both | Source/unit passed — renderer DOM and sizing suite covers three width classes, LTR/RTL/mixed labels, 1×/2× metrics, share reallocation, navigation survival, and full accessible text; official production-client proof pending |
| AC-PROV-05 | Electron client | Prove identical or changed composite labels do not affect IDs, cursors, authentication, routing, ordering, permissions, quotas, close, or restore. | Official path | Pending |
| AC-PROV-06 | Electron client | Make Back/Forward announce the full provenance reached; expose the pill as non-interactive with its full localized semantic name. | Official path | OpenClaw production path passed; Tightbeam path pending |

## Apple clients

The row-level mapping below integrates the frozen T1778 Apple canonical-AC
proposal, SHA-256
`0626f8729c98c6681e8ab083aaa38b437a754c38d858d3037100584c47397567`.
It maps all 72 canonical IDs in contract order. Named tests are exact current
anchors, not omnibus proof: every `R8-PENDING` entry remains pending until its
row-complete evidence exists.
The ten `APPLE-AC` entries remain summaries only.

R8 uses only the byte-identical installed Rust `surf-ace` CLI as actor and
evidence surface on Ansible and Aleph. XcodeBuildMCP device logs, screenshots,
and container hashes may corroborate CLI results but do not drive protocol
actions. No OpenClaw tool, MCP adapter, direct WebSocket/provider call, fixture,
test harness, or temporary proof client satisfies R8.

### Machine-readable conventions

- `APPLE-APPLICABLE`: the Apple amendment makes the client-side portion apply
  to both the admitted iPhone and iPad `SurfAce` builds.
- `APPLE-N/A`: the row is owned by a controller, standalone CLI/Tight Beam
  consumer, or review/proof process rather than by the Apple target client.
- `SWIFT-EXISTING`: one or more exact current XCTest methods directly exercise
  the row's native source/unit seam. Product/R8 proof may still be pending.
- `ELECTRON-EXISTING` has the corresponding meaning for cross-client parity.
- `R8-PENDING`: no evidence exists. The exact planned destination is the JSON
  pointer shown below.
- `R8-N/A`: no Apple real-device evidence is owed for that non-Apple row.

Swift test-file aliases used below resolve exactly as follows:

- `IOS-AUTH` = `packages/ios/SurfAceTests/SurfAceLocklessAuthorityTests.swift`
- `IOS-CONSUMABLE` = `packages/ios/SurfAceTests/SurfAceLocklessConsumableOperationsTests.swift`
- `IOS-CONTENT` = `packages/ios/SurfAceTests/SurfAceLocklessContentOperationsTests.swift`
- `IOS-RENDER` = `packages/ios/SurfAceTests/SurfAceRenderAndAnnotationDiagnosticsTests.swift`
- `IOS-RUNTIME` = `packages/ios/SurfAceTests/SurfAceLocklessRuntimeAdapterTests.swift`
- `IOS-TOPOLOGY` = `packages/ios/SurfAceTests/SurfAceLocklessTopologyOperationsTests.swift`

Electron test-file aliases used below resolve exactly as follows:

- `E-ACC` = `packages/electron/test/lockless-acceptance.test.ts`
- `E-AUTH` = `packages/electron/test/lockless-client-authority.test.ts`
- `E-WS` = `packages/electron/test/lockless-ws-server.test.ts`
- `E-MIG` = `packages/electron/test/migration-receipt.test.ts`
- `E-ROLL` = `packages/electron/test/legacy-rollback-migration.test.ts`
- `E-DOM` = `packages/electron/test/renderer-dom-integration.test.ts`
- `E-SIZE` = `packages/electron/test/renderer-sizing.test.ts`

The planned R8 manifest is explicitly a placeholder, not an existing artifact:

`<spec-root>/surf-ace/deployments/t1778-<merged-main-sha>-<UTC>/r8/apple-acceptance-evidence.json`

For each applicable row, the table points to
`#/rows/<AC-ID>/devices/{ansible,aleph}`. Each device object must contain
`status`, `installedApp`, `installedCli`, `controllerStateRoot`,
`cliEvidenceRefs`, `deviceCorroborationRefs`, and `assertions`. Each assertion
must contain `assertionId`, `result`, relevant request/receipt/commit/scope/
surface/pane IDs, and before/after hashes where the AC requires state or
restoration proof. The exact CLI transcript, device log, screenshot, and
container-manifest paths are referenced from those fields; they are not
invented here.

### Canonical 72-row Apple mapping

| Canonical AC | Apple applicability and rationale | Swift XCTest mapping at `b36bb66` | Electron parity mapping at `b36bb66` | R8 evidence destination/status |
|---|---|---|---|---|
| AC-ID-01 | `APPLE-N/A` — names OpenClaw and Tight Beam as the two active controller products; T1778 R8 instead requires shipped-CLI-exclusive actions with the extension passive. | `SWIFT-N/A` — controller-product scenario, not native client implementation. | `ELECTRON-N/A` — controller-product official-path obligation, not cross-client client-authority parity. | `R8-N/A` — forbidden R8 driver shape. |
| AC-ID-02 | `APPLE-APPLICABLE` — duplicate live stable-ID rejection and incumbent preservation are client registry semantics. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACID02DuplicateLiveIdentityPreservesIncumbentCursorUntilReaping`. | `ELECTRON-EXISTING` — `E-AUTH :: controller instance is deconflicted and retained across restart as dormant`; `E-WS :: one stable controller may hold lifecycle and surface sessions but not duplicate a target`. | `R8-PENDING` — `#/rows/AC-ID-02/devices/{ansible,aleph}`. |
| AC-ID-03 | `APPLE-APPLICABLE` — human labels must be identity-neutral in the client. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACID03DuplicateHumanLabelsDoNotDedupeControllersOrCursors`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-ID-03 AC-PROV-05: duplicate and changed human labels do not grant authority or recover another controller bundle`. | `R8-PENDING` — `#/rows/AC-ID-03/devices/{ansible,aleph}`. |
| AC-ID-04 | `APPLE-N/A` — this row exercises controller restart/redeploy, controller-local durable identity, and projection resume. | `SWIFT-N/A` — controller-side durable state, not Apple client state. | `ELECTRON-N/A` — controller-side behavior rather than Electron client parity. | `R8-N/A` — controller implementation row. |
| AC-ARCH-01 | `APPLE-APPLICABLE` — the Apple client must remain authoritative/actionable when one controller disappears. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACARCH01SurvivorRemainsActionableAcrossPeerPartitionAndRestart`. | `ELECTRON-EXISTING` — `E-ACC :: AC-ARCH-01 AC-SYNC-01 AC-SYNC-02 AC-SYNC-03 AC-READ-02: one client authority refuses offline writes while the surviving controller remains actionable`. | `R8-PENDING` — `#/rows/AC-ARCH-01/devices/{ansible,aleph}`. |
| AC-ARCH-02 | `APPLE-N/A` — system/config inspection of Tight Beam, coordinator absence, and compositor ownership is not an Apple-target semantic check. | `SWIFT-N/A` — architecture-review row. | `ELECTRON-N/A` — broader system/compositor source inspection, not Electron parity test. | `R8-N/A` — Gate-3/review evidence, not R8. |
| AC-SYNC-01 | `APPLE-APPLICABLE` — disconnected writes must never enter Apple authority. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACSYNC01DisconnectedContentAndTopologyWritesNeverEnterAuthority`. | `ELECTRON-EXISTING` — `E-ACC :: AC-ARCH-01 AC-SYNC-01 AC-SYNC-02 AC-SYNC-03 AC-READ-02: one client authority refuses offline writes while the surviving controller remains actionable`. | `R8-PENDING` — `#/rows/AC-SYNC-01/devices/{ansible,aleph}`. |
| AC-SYNC-02 | `APPLE-APPLICABLE` — one authoritative native replica and no optimistic writable copy is a client boundary. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACSYNC02OnePersistedAuthorityReplicaOwnsRemoteAndLocalWrites`. | `ELECTRON-EXISTING` — `E-ACC :: AC-ARCH-01 AC-SYNC-01 AC-SYNC-02 AC-SYNC-03 AC-READ-02: one client authority refuses offline writes while the surviving controller remains actionable`. | `R8-PENDING` — `#/rows/AC-SYNC-02/devices/{ansible,aleph}` is corroboration only; Gate-3 source review remains authoritative. |
| AC-SYNC-03 | `APPLE-APPLICABLE` — an Apple implementation must preserve the conformance tripwire against offline/multi-authoritative writes. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACSYNC03OfflineAndIncompleteCapabilityTripwiresRejectBeforeMutation`. | `ELECTRON-EXISTING` — `E-ACC :: AC-ARCH-01 AC-SYNC-01 AC-SYNC-02 AC-SYNC-03 AC-READ-02: one client authority refuses offline writes while the surviving controller remains actionable`. | `R8-PENDING` — `#/rows/AC-SYNC-03/devices/{ansible,aleph}` is corroboration only; Gate-3 source review remains authoritative. |
| AC-HIST-01 | `APPLE-APPLICABLE` — client-authored mixed-controller history ordering is shared authority. | `SWIFT-EXISTING` — `IOS-CONTENT :: SurfAceLocklessContentOperationsTests.testACHIST01InterleavedControllersShareOrderedBackForwardHistory`. | `ELECTRON-EXISTING` — `E-ACC :: AC-HIST-01..05: production SurfaceCore appends mixed-controller history, restores entry state, truncates Forward, and applies cross-controller LRU`. | `R8-PENDING` — `#/rows/AC-HIST-01/devices/{ansible,aleph}`. |
| AC-HIST-02 | `APPLE-APPLICABLE` — the shared non-visible LRU pool is native client state. | `SWIFT-EXISTING` — `IOS-CONTENT :: SurfAceLocklessContentOperationsTests.testACHIST02NavigationRecencyControlsSharedTwentyEntryLRU`. | `ELECTRON-EXISTING` — `E-ACC :: AC-HIST-01..05: production SurfaceCore appends mixed-controller history, restores entry state, truncates Forward, and applies cross-controller LRU`. | `R8-PENDING` — `#/rows/AC-HIST-02/devices/{ansible,aleph}`. |
| AC-HIST-03 | `APPLE-APPLICABLE` — Forward truncation before LRU is client behavior. | `SWIFT-EXISTING` — `IOS-CONTENT :: SurfAceLocklessContentOperationsTests.testACHIST03BackThenPushClearsForwardIndependentOfController`. | `ELECTRON-EXISTING` — `E-ACC :: AC-HIST-01..05: production SurfaceCore appends mixed-controller history, restores entry state, truncates Forward, and applies cross-controller LRU`. | `R8-PENDING` — `#/rows/AC-HIST-03/devices/{ansible,aleph}`. |
| AC-HIST-04 | `APPLE-APPLICABLE` — exact entry state/provenance restore is native client/UI behavior. | `SWIFT-EXISTING` — `IOS-CONTENT :: SurfAceLocklessContentOperationsTests.testACHIST04NavigationRestoresEntryStateWithoutCursorMovement`. | `ELECTRON-EXISTING` — `E-ACC :: AC-HIST-01..05: production SurfaceCore appends mixed-controller history, restores entry state, truncates Forward, and applies cross-controller LRU`; `E-SIZE :: renderer chrome keeps entry-bound composite provenance in navigation chrome`. | `R8-PENDING` — `#/rows/AC-HIST-04/devices/{ansible,aleph}`. |
| AC-HIST-05 | `APPLE-APPLICABLE` — cross-controller LRU must be identity-neutral. | `SWIFT-EXISTING` — `IOS-CONTENT :: SurfAceLocklessContentOperationsTests.testACHIST05ChattyControllerHasNoHistoryQuotaOrProtection`. | `ELECTRON-EXISTING` — `E-ACC :: AC-HIST-01..05: production SurfaceCore appends mixed-controller history, restores entry state, truncates Forward, and applies cross-controller LRU`. | `R8-PENDING` — `#/rows/AC-HIST-05/devices/{ansible,aleph}`. |
| AC-PROV-01 | `APPLE-APPLICABLE` — the amendment explicitly requires native presentation of the captured composite. | `SWIFT-EXISTING` — `IOS-RENDER :: SurfAceRenderAndAnnotationDiagnosticsTests.testACPROV01ExactFriendlyChatAndProductComposite`. | `ELECTRON-EXISTING` — `E-DOM :: renderer DOM integrates authoritative connection states and live scale controls` asserts exact `OpenClaw — Clawline`. | `R8-PENDING` — `#/rows/AC-PROV-01/devices/{ansible,aleph}`. |
| AC-PROV-02 | `APPLE-APPLICABLE` — native Back/Forward presentation must use entry-bound rather than current metadata. | `SWIFT-EXISTING` — `IOS-RENDER :: SurfAceRenderAndAnnotationDiagnosticsTests.testACPROV02CompositeIsCapturedByVisibleEntryAcrossNavigation`. | `ELECTRON-EXISTING` — `E-SIZE :: renderer chrome keeps entry-bound composite provenance in navigation chrome`; `E-DOM :: renderer DOM integrates authoritative connection states and live scale controls`. | `R8-PENDING` — `#/rows/AC-PROV-02/devices/{ansible,aleph}`. |
| AC-PROV-03 | `APPLE-APPLICABLE` — localized two-part fallbacks are native client UI semantics. | `SWIFT-EXISTING` — `IOS-RENDER :: SurfAceRenderAndAnnotationDiagnosticsTests.testACPROV03LocalizedFallbacksTrimButDoNotTranslateSuppliedLabels`. | `ELECTRON-EXISTING` — `E-DOM :: renderer DOM integrates authoritative connection states and live scale controls`. | `R8-PENDING` — `#/rows/AC-PROV-03/devices/{ansible,aleph}`. |
| AC-PROV-04 | `APPLE-APPLICABLE` — width classes, bidi handling, Dynamic Type, navigation survival, and full accessible text apply to native presentation. | `SWIFT-EXISTING` — `IOS-RENDER :: SurfAceRenderAndAnnotationDiagnosticsTests.testACPROV04WidthClassesAndBidirectionalIsolationAreDeterministic`. | `ELECTRON-EXISTING` — `E-DOM :: renderer DOM integrates authoritative connection states and live scale controls`. | `R8-PENDING` — `#/rows/AC-PROV-04/devices/{ansible,aleph}`. |
| AC-PROV-05 | `APPLE-APPLICABLE` — composite labels must not alter authority. | `SWIFT-EXISTING` — `IOS-RENDER :: SurfAceRenderAndAnnotationDiagnosticsTests.testACPROV05CompositeChangesDoNotChangeAuthorityIdentityOrCursorKeys`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-ID-03 AC-PROV-05: duplicate and changed human labels do not grant authority or recover another controller bundle`. | `R8-PENDING` — `#/rows/AC-PROV-05/devices/{ansible,aleph}`. |
| AC-PROV-06 | `APPLE-APPLICABLE` — native assistive announcements and non-interactive pill semantics are explicitly required. | `SWIFT-EXISTING` — `IOS-RENDER :: SurfAceRenderAndAnnotationDiagnosticsTests.testACPROV06FullAccessibleAnnouncementNeverCollapses`. | `ELECTRON-EXISTING` — `E-DOM :: renderer DOM integrates authoritative connection states and live scale controls`. | `R8-PENDING` — `#/rows/AC-PROV-06/devices/{ansible,aleph}`. |
| AC-TOPO-01 | `APPLE-APPLICABLE` — same-revision serialization is client authority. | `SWIFT-EXISTING` — `SurfAceLocklessTopologyOperationsTests.testSameRevisionCommitsExactlyOneMutationAndReturnsCurrentTreeToLoser`. | `ELECTRON-EXISTING` — `E-ACC :: AC-TOPO-01 AC-TOPO-02 AC-TOPO-03 AC-TOPO-05 AC-TOPO-06 AC-OPS-02: stale topology intent is never rebased retried or transformed and exact receipts replay`. | `R8-PENDING` — `#/rows/AC-TOPO-01/devices/{ansible,aleph}`. |
| AC-TOPO-02 | `APPLE-APPLICABLE` — stale losers must explicitly refresh/recompute/new-ID retry. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACTOPO02StaleLoserMustRefreshRecomputeAndUseFreshRevision`. | `ELECTRON-EXISTING` — `E-ACC :: AC-TOPO-01 AC-TOPO-02 AC-TOPO-03 AC-TOPO-05 AC-TOPO-06 AC-OPS-02: stale topology intent is never rebased retried or transformed and exact receipts replay`. | `R8-PENDING` — `#/rows/AC-TOPO-02/devices/{ansible,aleph}`. |
| AC-TOPO-03 | `APPLE-APPLICABLE` — the native client must not rebase/merge/transform/retry stale intent. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACTOPO03StalePayloadIsNeverPartiallyAppliedOrRetried`. | `ELECTRON-EXISTING` — `E-ACC :: AC-TOPO-01 AC-TOPO-02 AC-TOPO-03 AC-TOPO-05 AC-TOPO-06 AC-OPS-02: stale topology intent is never rebased retried or transformed and exact receipts replay`. | `R8-PENDING` — `#/rows/AC-TOPO-03/devices/{ansible,aleph}`; Gate-3 source inspection remains required. |
| AC-TOPO-04 | `APPLE-APPLICABLE` — all native topology operations must share stable IDs and one revision seam. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACTOPO04EveryOperationUsesStableIDsAndOneRevisionSeam`. | `ELECTRON-EXISTING` — `E-WS :: AC-TOPO-04: split rename resize close restore and realization share stable IDs and one topology revision seam`. | `R8-PENDING` — `#/rows/AC-TOPO-04/devices/{ansible,aleph}`. |
| AC-TOPO-05 | `APPLE-APPLICABLE` — client receipt order, not identity/labels, owns commit/allocation order. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACTOPO05AllocationFollowsSerializedReceiptOrderNotLabels`. | `ELECTRON-EXISTING` — `E-ACC :: AC-TOPO-01 AC-TOPO-02 AC-TOPO-03 AC-TOPO-05 AC-TOPO-06 AC-OPS-02: stale topology intent is never rebased retried or transformed and exact receipts replay`. | `R8-PENDING` — `#/rows/AC-TOPO-05/devices/{ansible,aleph}`. |
| AC-TOPO-06 | `APPLE-APPLICABLE` — request replay/reuse semantics are native client receipt behavior. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACTOPO06ReceiptReplayChangedPayloadReuseAndFreshIDRecompute`. | `ELECTRON-EXISTING` — `E-ACC :: AC-TOPO-01 AC-TOPO-02 AC-TOPO-03 AC-TOPO-05 AC-TOPO-06 AC-OPS-02: stale topology intent is never rebased retried or transformed and exact receipts replay`. | `R8-PENDING` — `#/rows/AC-TOPO-06/devices/{ansible,aleph}`. |
| AC-CAP-01 | `APPLE-APPLICABLE` — pane-creation cap, P+T conservation, and capacity-free restore are client state/accounting. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACCAP01CreationCapRetainedConservationAndCapacityFreeRestore`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-01 AC-CLOSE-01..08: P/T conservation permits exact restore over P and stale/invalid failures preserve tombstones atomically`. | `R8-PENDING` — `#/rows/AC-CAP-01/devices/{ansible,aleph}`. |
| AC-CAP-02 | `APPLE-APPLICABLE` — exact annotation/pane/surface bytes and atomic refusal are native authority. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACCAP02ExactPaneAnnotationAndSurfaceBaseBoundsAreAtomicAndRestorable`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-02 AC-CLOSE-06 AC-CLOSE-09: exact byte limits accept equality, reject +1 silently, and report oldest tombstone reclamation`; `E-ACC :: CAP-3 websocket mutations reject exact surface and pane byte classes atomically`. | `R8-PENDING` — `#/rows/AC-CAP-02/devices/{ansible,aleph}`. |
| AC-CAP-03 | `APPLE-APPLICABLE` — the complete recoverable envelope and over-P restore are native persistence/admission semantics. | `SWIFT-EXISTING` — shared exact fixture `IOS-AUTH :: SurfAceLocklessAuthorityTests.testACCAP03AndACOPS01ExactOverPGenerationRestoresBeforeAdmission`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-03 AC-SURF-06: SURF-12 is exact and rejects a one-byte-short recoverable envelope`. | `R8-PENDING` — `#/rows/AC-CAP-03/devices/{ansible,aleph}`. |
| AC-CLOSE-01 | `APPLE-APPLICABLE` — pane close/tombstone content, history, annotations, scopes, and cursors are client truth. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACCLOSE01CloseAtomicallyPreservesMixedHistoryProvenanceAnnotationsAndUnreadCursors`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-01 AC-CLOSE-01..08: P/T conservation permits exact restore over P and stale/invalid failures preserve tombstones atomically`. | `R8-PENDING` — `#/rows/AC-CLOSE-01/devices/{ansible,aleph}`. |
| AC-CLOSE-02 | `APPLE-APPLICABLE` — any controller must restore exact pane state even at/above P. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACCLOSE02AnyControllerRestoresExactPaneAtAndAboveCreationCap`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-01 AC-CLOSE-01..08: P/T conservation permits exact restore over P and stale/invalid failures preserve tombstones atomically`. | `R8-PENDING` — `#/rows/AC-CLOSE-02/devices/{ansible,aleph}`. |
| AC-CLOSE-03 | `APPLE-APPLICABLE` — stale/invalid restore must preserve the tombstone. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACCLOSE03StaleAndInvalidPlacementPreserveTombstoneUntilFreshRetry`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-01 AC-CLOSE-01..08: P/T conservation permits exact restore over P and stale/invalid failures preserve tombstones atomically`. | `R8-PENDING` — `#/rows/AC-CLOSE-03/devices/{ansible,aleph}`. |
| AC-CLOSE-04 | `APPLE-APPLICABLE` — restore preserves stable pane ID but reallocates a conflicting visible label. | `SWIFT-EXISTING` — `SurfAceLocklessTopologyOperationsTests.testPaneCloseRestorePreservesIdentityStateAndAllocatesConflictingLabel`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-01 AC-CLOSE-01..08: P/T conservation permits exact restore over P and stale/invalid failures preserve tombstones atomically`. | `R8-PENDING` — `#/rows/AC-CLOSE-04/devices/{ansible,aleph}`. |
| AC-CLOSE-05 | `APPLE-APPLICABLE` — last-live-pane refusal is identity-neutral native authority. | `SWIFT-EXISTING` — `SurfAceLocklessTopologyOperationsTests.testCloseLastPaneIsIdentityNeutralAndAtomic`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-01 AC-CLOSE-01..08: P/T conservation permits exact restore over P and stale/invalid failures preserve tombstones atomically`. | `R8-PENDING` — `#/rows/AC-CLOSE-05/devices/{ansible,aleph}`. |
| AC-CLOSE-06 | `APPLE-APPLICABLE` — count/byte-bounded oldest tombstone reclamation is native authority. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACCLOSE06GlobalCountAndBytePressureReclaimsAscendingClosedSequence`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-02 AC-CLOSE-06 AC-CLOSE-09: exact byte limits accept equality, reject +1 silently, and report oldest tombstone reclamation`. | `R8-PENDING` — `#/rows/AC-CLOSE-06/devices/{ansible,aleph}`. |
| AC-CLOSE-07 | `APPLE-APPLICABLE` — native authority must prove close-never-refuses under SURF-12 and atomic aggregate-pool rejection at admission/migration/restart/configuration. | `SWIFT-EXISTING` — `IOS-AUTH :: SurfAceLocklessAuthorityTests.testACCLOSE07ValidCloseAtExactBoundAndFourStandaloneTransitionsAreAtomic`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-CLOSE-07: a valid admitted close at the retained byte bound succeeds without tombstone_capacity under identity reversal`; `E-AUTH :: AC-CLOSE-07: admission migration restart and configuration reject aggregate bound+1 atomically and accept equality under identity reversal`. | `R8-PENDING` — `#/rows/AC-CLOSE-07/devices/{ansible,aleph}`. |
| AC-CLOSE-08 | `APPLE-APPLICABLE` — reads/restores must not refresh reclamation order. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACCLOSE08ReadsAndFailedRestoreDoNotRefreshReclamationOrder`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-01 AC-CLOSE-01..08: P/T conservation permits exact restore over P and stale/invalid failures preserve tombstones atomically`. | `R8-PENDING` — `#/rows/AC-CLOSE-08/devices/{ansible,aleph}`. |
| AC-CLOSE-09 | `APPLE-APPLICABLE` — reclamation event order and durable diagnostics are client output. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACCLOSE09ReclamationDiagnosticsContainStableIdentityBoundsAndReason`; `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACCLOSE09TombstoneReclamationOutboxPersistsOrderedCommitRecipientsUntilDelivery`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-02 AC-CLOSE-06 AC-CLOSE-09: exact byte limits accept equality, reject +1 silently, and report oldest tombstone reclamation`. | `R8-PENDING` — `#/rows/AC-CLOSE-09/devices/{ansible,aleph}`. |
| AC-SURF-01 | `APPLE-APPLICABLE` — admitted `SurfAce` implements client/local surface lifecycle; local-user recoverable close is unconditional. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACSURF01ControllerAndLocalOpenShareStableCallerNeutralLifecycleSeam`. | `ELECTRON-EXISTING` — `E-WS :: AC-SURF-01: controller and local-user surface lifecycle share the persisted client authority seam`. | `R8-PENDING` — `#/rows/AC-SURF-01/devices/{ansible,aleph}`. |
| AC-SURF-02 | `APPLE-APPLICABLE` — full surface close/restore, nested tombstones, unread state, and event/socket ordering are client semantics. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACSURF02SurfaceCloseRestorePreservesNestedTombstonesHistoryAndUnreadScopes`. | `ELECTRON-EXISTING` — `E-WS :: AC-SURF-02: complete surface close persists a tombstone before zero-live socket teardown and restores exact identity after restart`. | `R8-PENDING` — `#/rows/AC-SURF-02/devices/{ansible,aleph}`. |
| AC-SURF-03 | `APPLE-APPLICABLE` — local-user last-surface close, zero-live discovery, and restore are explicitly required on Apple. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACSURF03LastLocalSurfaceCloseKeepsZeroLiveEndpointAndCallerNeutralRestore`. | `ELECTRON-EXISTING` — `E-WS :: AC-SURF-02: complete surface close persists a tombstone before zero-live socket teardown and restores exact identity after restart`; `E-WS :: AC-SURF-01: controller and local-user surface lifecycle share the persisted client authority seam`. | `R8-PENDING` — `#/rows/AC-SURF-03/devices/{ansible,aleph}`. |
| AC-SURF-04 | `APPLE-APPLICABLE` — concurrent lifecycle serialization and explicit stale recomputation belong to client authority. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACSURF04ConcurrentLifecycleLosersRemainAtomicUntilFreshRevisionRetry`. | `ELECTRON-EXISTING` — `E-WS :: AC-SURF-04: concurrent lifecycle requests serialize once and stale callers must recompute with a new request ID`. | `R8-PENDING` — `#/rows/AC-SURF-04/devices/{ansible,aleph}`. |
| AC-SURF-05 | `APPLE-APPLICABLE` — bounded surface close and global tombstone reclamation are client accounting. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACSURF05SurfaceCloseCountsNestedStateOnceAndPreservesExistingGap`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-SURF-05: bounded global surface tombstone reclamation follows closed sequence and audits nested unread disposition`. | `R8-PENDING` — `#/rows/AC-SURF-05/devices/{ansible,aleph}`. |
| AC-SURF-06 | `APPLE-APPLICABLE` — zero-live over-P restart restoration before admission is native persistence. | `SWIFT-EXISTING` — `IOS-TOPOLOGY :: SurfAceLocklessTopologyOperationsTests.testACSURF06ZeroLiveOverPGenerationRestoresBeforeCreationAdmission`. | `ELECTRON-EXISTING` — `E-ACC :: AC-CAP-03 AC-SURF-06: SURF-12 is exact and rejects a one-byte-short recoverable envelope`; `E-WS :: AC-SURF-02: complete surface close persists a tombstone before zero-live socket teardown and restores exact identity after restart`. | `R8-PENDING` — `#/rows/AC-SURF-06/devices/{ansible,aleph}`. |
| AC-RET-01 | `APPLE-APPLICABLE` — dormant order, exact cursor resume, new sequence, and fresh-tail admission are client registry semantics. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACRET01DormantOrderIgnoresReadsAndLabelsThenResumeKeepsCursors`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-RET-01: dormant resume preserves its exact cursor while reclaimed readmission starts at the current tail`. | `R8-PENDING` — `#/rows/AC-RET-01/devices/{ansible,aleph}`. |
| AC-RET-02 | `APPLE-APPLICABLE` — exact dormant count/byte/total bounds and identity-blind victims are client retention. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACRET02CountByteAndRestartPressureAlwaysReclaimsOldestSequence`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-RET-02: dormant count byte and total reclamation chooses the oldest sequence and reports exact unread disposition`. | `R8-PENDING` — `#/rows/AC-RET-02/devices/{ansible,aleph}`. |
| AC-RET-03 | `APPLE-APPLICABLE` — atomic registry/cursor/frame reclamation and exact unread disposition are client authority. | `SWIFT-EXISTING` — `SurfAceLocklessAuthorityTests.testReclamationCapturesExactLiveAndTombstoneUnreadDispositionBeforeDeletion`; `SurfAceLocklessRuntimeAdapterTests.testDormantCountPressureReclaimsOldestAndReadmissionStartsAtCurrentTail`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-RET-02: dormant count byte and total reclamation chooses the oldest sequence and reports exact unread disposition`. | `R8-PENDING` — `#/rows/AC-RET-03/devices/{ansible,aleph}`. |
| AC-RET-04 | `APPLE-APPLICABLE` — live-only capacity refusal and oldest eligible dormant reclamation are client admission. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACRET04LiveOnlyCapacityRefusesThenDormantAdmissionReclaimsAcrossRestart`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-RET-04: all-live registry refuses admission and a later admission reclaims only the oldest eligible dormant bundle`. | `R8-PENDING` — `#/rows/AC-RET-04/devices/{ansible,aleph}`. |
| AC-LIVEBUF-01 | `APPLE-APPLICABLE` — bounded scopes, targeted cursor movement/gaps/events, and loss diagnostics are native client authority. | `SWIFT-EXISTING` — `IOS-CONSUMABLE :: SurfAceLocklessConsumableOperationsTests.testACLIVEBUF01ConnectedNeverReaderGetsTargetedStickyGapWhileProducerContinues`. | `ELECTRON-EXISTING` — `E-ACC :: AC-LIVEBUF-01 AC-LIVEBUF-02 AC-OPS-02: overflow targets only lagging cursors and emits ordered durable correlation`. | `R8-PENDING` — `#/rows/AC-LIVEBUF-01/devices/{ansible,aleph}`. |
| AC-LIVEBUF-02 | `APPLE-APPLICABLE` — all declared coalescing/append/oversize/target-result/close-restore semantics are client-owned. | `SWIFT-EXISTING` — `IOS-CONSUMABLE :: SurfAceLocklessConsumableOperationsTests.testACLIVEBUF02DeclaredCoalescingTargetResultsOversizeReplayAndAckStayDistinct`. | `ELECTRON-EXISTING` — `E-ACC :: AC-LIVEBUF-01 AC-LIVEBUF-02 AC-OPS-02: overflow targets only lagging cursors and emits ordered durable correlation`; `E-AUTH :: bounded consumables preserve independent cursors and sticky structured gaps`; `E-AUTH :: target apply work survives restart and terminalizes as correlated append-only surface truth`. | `R8-PENDING` — `#/rows/AC-LIVEBUF-02/devices/{ansible,aleph}`. |
| AC-LIVEBUF-03 | `APPLE-N/A` — this is the controller's durable local projection, offline ordinary read, and acknowledgement outbox. | `SWIFT-N/A` — controller-side state, not Apple client authority. | `ELECTRON-N/A` — controller-side behavior, not Electron client parity. | `R8-N/A` — proven by shipped CLI/controller state rather than an Apple AC entry. |
| AC-READ-01 | `APPLE-N/A` — ordinary local read and later controller acknowledgement delivery are controller projection behavior. | `SWIFT-N/A` — controller-side state. | `ELECTRON-N/A` — controller-side behavior. | `R8-N/A` — controller proof, not Apple-client mapping. |
| AC-READ-02 | `APPLE-APPLICABLE` — client commits must continue for a slow/disconnected controller and expose bounded lag/loss. | `SWIFT-EXISTING` — `IOS-CONSUMABLE :: SurfAceLocklessConsumableOperationsTests.testACREAD02WithinWindowCatchesUpAndCrossingWindowUsesStructuredLoss`. | `ELECTRON-EXISTING` — `E-ACC :: AC-ARCH-01 AC-SYNC-01 AC-SYNC-02 AC-SYNC-03 AC-READ-02: one client authority refuses offline writes while the surviving controller remains actionable`. | `R8-PENDING` — `#/rows/AC-READ-02/devices/{ansible,aleph}`. |
| AC-MIG-01 | `APPLE-APPLICABLE` — target capability gating and mutually exclusive legacy/lockless mode are explicit Apple obligations. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACMIG01NativeTargetGateSeparatesLegacyLocklessAndSpatial`. | `ELECTRON-EXISTING` — `E-WS :: AC-MIG-01: upgraded Electron keeps legacy and lockless negotiation mutually exclusive without capable fallback`. | `R8-PENDING` — `#/rows/AC-MIG-01/devices/{ansible,aleph}` plus separate Gate-7 unchanged-legacy evidence. |
| AC-MIG-02 | `APPLE-APPLICABLE` — legacy material conversion, exact bounds, over-P acceptance, and no trim/owner selection are native migration semantics. | `SWIFT-EXISTING` — `IOS-AUTH :: SurfAceLocklessAuthorityTests.testACMIG02OverPPreservedStateMigratesWithoutClampAndTrueCombinedOverflowRejects`. | `ELECTRON-EXISTING` — `E-AUTH :: legacy migration imports gaps transactionally and rolls back oversize material`; `E-ACC :: AC-MIG-02: renderer geometry does not preserve the non-positive bootstrap pane during lockless admission`. | `R8-PENDING` — `#/rows/AC-MIG-02/devices/{ansible,aleph}`. |
| AC-MIG-03 | `APPLE-APPLICABLE` — this row activates the complete applicable check set independently on iPhone and iPad. | `SWIFT-EXISTING` — platform aggregate over the row-complete XCTest sources mapped in all other applicable Swift cells: exact-source `SurfAce` iPhone simulator **205/205**, exact-source `SurfAce` iPad simulator **205/205**, `SurfAceLegacyRollback` **3/3**, and non-capable `SurfAceSpatial` visionOS simulator build success. | `ELECTRON-EXISTING` — `E-MIG :: AC-MIG-03: Electron platform gate enumerates every formerly pending canonical parity row in the built suite`; `packages/electron/scripts/build.mjs` includes that manifest test and every mapped Electron test source; the complete Electron gate is `pnpm --filter @surf-ace/electron run build && pnpm --filter @surf-ace/electron test`, which executes `dist/test/*.test.js`. | `R8-PENDING` — `#/rows/AC-MIG-03/devices/{ansible,aleph}` must reference every applicable row result and exact merged-source test/build manifests. |
| AC-MIG-04 | `APPLE-N/A` — standalone CLI, Tight Beam ordinary-archetype consumption, and OpenClaw controller routing are controller/consumer obligations. | `SWIFT-N/A` — Apple is only the target client. | `ELECTRON-N/A` — controller/consumer route proof, not Electron parity. | `R8-N/A` — R8 forbids Tight Beam/OpenClaw action drivers. |
| AC-MIG-05 | `APPLE-APPLICABLE` — stopped-app rollback must preserve/report material without selecting an owner. | `SWIFT-EXISTING` — `SurfAceLocklessAuthorityTests.testRollbackPreviewIsDeterministicAndDoesNotMutateAuthority`; `SurfAceAppleRollbackTests.testPreviewIsStableNonmutatingAndProjectsRepresentableMaterialWithoutOwner`; `SurfAceAppleRollbackTests.testApplyMutatesOnlyExactCopyAndRestoreRecoversOriginalFileBytes`; `SurfAceAppleRollbackTests.testApplyRequiresAnUntouchedOperatorSuppliedCopyAndRetainedPriorApp`. | `ELECTRON-EXISTING` — `E-ROLL :: preview preserves legacy material without an owner and reports every lockless-only item`; `E-ROLL :: offline preview/apply installs legacy state and captured generation restores exactly`. | `R8-PENDING` — `#/rows/AC-MIG-05/devices/{ansible,aleph}` must reference preview/apply/restore manifests; destructive rollback execution remains governed by Gate 5/10. |
| AC-OPS-01 | `APPLE-APPLICABLE` for complete client-generation restore before admission; the later CLI cache/outbox clause remains controller-owned. | `SWIFT-EXISTING` — shared exact fixture `IOS-AUTH :: SurfAceLocklessAuthorityTests.testACCAP03AndACOPS01ExactOverPGenerationRestoresBeforeAdmission`. | `ELECTRON-EXISTING` — `E-AUTH :: AC-OPS-01: restart restores limits modes controller bundles consumable scopes sequences and tombstones before admission`; controller-cache clause is N/A to Electron. | `R8-PENDING` — `#/rows/AC-OPS-01/devices/{ansible,aleph}`. |
| AC-OPS-02 | `APPLE-APPLICABLE` — stable codes, one commit, and ordered fan-out are native client diagnostics/audit. | `SWIFT-EXISTING` — `IOS-RUNTIME :: SurfAceLocklessRuntimeAdapterTests.testACOPS02MutationsOverflowAndReclamationHaveStableCommitCorrelation`. | `ELECTRON-EXISTING` — `E-ACC :: AC-TOPO-01 AC-TOPO-02 AC-TOPO-03 AC-TOPO-05 AC-TOPO-06 AC-OPS-02: stale topology intent is never rebased retried or transformed and exact receipts replay`; `E-ACC :: AC-LIVEBUF-01 AC-LIVEBUF-02 AC-OPS-02: overflow targets only lagging cursors and emits ordered durable correlation`. | `R8-PENDING` — `#/rows/AC-OPS-02/devices/{ansible,aleph}`. |
| AC-PROOF-01 | `APPLE-N/A` as an implementation row — it governs official OpenClaw/Tight Beam proof doctrine; T1778 Apple product proof is separately constrained to the shipped CLI by R8/R11. | `SWIFT-N/A` — proof-process rule, not native source semantics. | `ELECTRON-N/A` — proof doctrine, not an Electron parity test. | `R8-N/A` — R8 uses its stricter shipped-CLI-only doctrine. |
| AC-PROOF-02 | `APPLE-N/A` — canonical design/compositor amendment and implementation-review synchronization gate. | `SWIFT-N/A` — documentation/review gate. | `ELECTRON-N/A` — documentation/review gate. | `R8-N/A` — Gate-3 source/review evidence. |
| AC-CLI-01 | `APPLE-N/A` — Rust CLI macOS/Linux build and vector parity. | `SWIFT-N/A` — CLI build row. | `ELECTRON-N/A` — CLI build row. | `R8-N/A` — installed CLI identity is recorded globally, not as Apple client acceptance. |
| AC-CLI-02 | `APPLE-N/A` — CLI/package shape and prohibited MCP/Tight-Beam-specific surfaces. | `SWIFT-N/A` — CLI/package inspection. | `ELECTRON-N/A` — CLI/package inspection. | `R8-N/A` — Gate-3/6 CLI package evidence. |
| AC-CLI-03 | `APPLE-N/A` — standalone and ordinary-archetype identical-binary CLI proof. | `SWIFT-N/A` — controller/consumer row. | `ELECTRON-N/A` — controller/consumer row. | `R8-N/A` — R8 forbids Tight Beam wrapper evidence. |
| AC-CLI-04 | `APPLE-N/A` — CLI durable controller identity and whole-invocation serialization. | `SWIFT-N/A` — controller implementation. | `ELECTRON-N/A` — controller implementation. | `R8-N/A` — controller state-root evidence. |
| AC-CLI-05 | `APPLE-N/A` — CLI persistence/network interruption matrix; target-work portions remain exercised through Apple client rows, not this CLI row. | `SWIFT-N/A` — canonical row is CLI-owned. | `ELECTRON-N/A` — canonical row is CLI-owned. | `R8-N/A` — controller crash-window proof is recorded under the CLI proof package. |
| AC-CLI-06 | `APPLE-N/A` — zero-network ordinary CLI read. | `SWIFT-N/A` — controller projection. | `ELECTRON-N/A` — controller projection. | `R8-N/A` — controller state-root proof. |
| AC-CLI-07 | `APPLE-N/A` — CLI acknowledgement/outbox/receipt reconciliation. | `SWIFT-N/A` — controller projection. | `ELECTRON-N/A` — controller projection. | `R8-N/A` — controller state-root proof. |
| AC-CLI-08 | `APPLE-N/A` — deterministic CLI command JSON and request/receipt/result correlation. | `SWIFT-N/A` — CLI command contract. | `ELECTRON-N/A` — CLI command contract. | `R8-N/A` — referenced by applicable Apple rows' `cliEvidenceRefs`, not duplicated as Apple AC. |
| AC-CLI-09 | `APPLE-N/A` — independent T1770 correction review artifact. | `SWIFT-N/A` — review gate. | `ELECTRON-N/A` — review gate. | `R8-N/A` — pre-R8 review evidence. |
| AC-CLI-10 | `APPLE-N/A` — historical reversible tablet-a/Tight Beam consumer proof; tablet-a cannot satisfy T1778 R8. | `SWIFT-N/A` — controller/consumer official path. | `ELECTRON-N/A` — controller/consumer official path. | `R8-N/A` — explicitly excluded by Apple amendment non-goals. |

### APPLE-AC summaries (not substitutes for the 72-row matrix)

These ten entries remain aggregate navigation aids only. They add no canonical
acceptance IDs and must not be counted as mapped rows.

| Summary ID | Summary only |
|---|---|
| APPLE-AC-01 | Complete capability/finite-limit target gate on `SurfAce`; mutually exclusive legacy/lockless modes; `SurfAceSpatial` and unupgraded binaries remain non-capable. |
| APPLE-AC-02 | Distinct live controller IDs, duplicate-live rejection, dormant resume, and identity-blind reclamation. |
| APPLE-AC-03 | One persisted FIFO seam for remote/local mutations, client allocations, exact receipts, history/provenance, and fan-out. |
| APPLE-AC-04 | Stable-ID topology stale/retry, capacity, recoverable close/tombstones, restore, and local-user close. |
| APPLE-AC-05 | Controller-local snapshots/cursors/acks, retained records/live frames, structured gaps, order, and bounds. |
| APPLE-AC-06 | Complete generation and target-work recovery before admission across lifecycle/process restart and zero-live discovery. |
| APPLE-AC-07 | Durable `intent_committed -> materializing -> terminal` target work with exact receipt/result correlation and no duplicate materialization. |
| APPLE-AC-08 | Exact legacy migration plus stopped-app rollback preview/apply and byte-exact original-container restoration. |
| APPLE-AC-09 | Exact reviewed/merged source, build/install/launch/container/process chain, and executable Ansible/Aleph rollback. |
| APPLE-AC-10 | One canonical cross-language vector, iPhone/iPad gates, and non-capable `SurfAceSpatial` regression build. |

### Count audit and pending inventory

- Canonical IDs: **72**.
- `APPLE-APPLICABLE`: **54**.
- `APPLE-N/A`: **18**.
- Unmapped canonical IDs: **0**.
- `SWIFT-EXISTING`: **54**; `SWIFT-PENDING`: **0**; `SWIFT-N/A`: **18**.
- `ELECTRON-EXISTING`: **54**; `ELECTRON-PENDING`: **0**;
  `ELECTRON-N/A`: **18**.
- R8 evidence present: **0**.
- `R8-PENDING`: **54**.
- `R8-N/A`: **18**.

The explicit Apple-N/A list is:

`AC-ID-01`, `AC-ID-04`, `AC-ARCH-02`, `AC-LIVEBUF-03`, `AC-READ-01`,
`AC-MIG-04`, `AC-PROOF-01`, `AC-PROOF-02`, `AC-CLI-01`, `AC-CLI-02`,
`AC-CLI-03`, `AC-CLI-04`, `AC-CLI-05`, `AC-CLI-06`, `AC-CLI-07`,
`AC-CLI-08`, `AC-CLI-09`, `AC-CLI-10`.

The `R8-PENDING` list is exactly the 54-row `APPLE-APPLICABLE` set in the
table. Every entry remains pending until its two device pointers resolve to
real hashed evidence; native source/unit and simulator gates do not satisfy R8.
## OpenClaw controller

OpenClaw also participates in every dual-controller and official-path row that names both controllers.

| AC ID | Primary lane | Required implementation and proof | Proof class | Status |
|---|---|---|---|---|
| AC-ID-04 | OpenClaw controller | Persist the controller ID across restart/redeploy and resume its retained dormant cursor; treat regenerated or reclaimed IDs as fresh at current tail without label inheritance. | Both | Pending |
| AC-LIVEBUF-03 | OpenClaw controller | Build the bounded local projection from pair/resume snapshots and ordered deltas; present one alert per burst; keep ordinary `surf_ace_read` network-free while atomically persisting projected consumption and a background acknowledgement; reconcile cache/outbox/sequences/pending/gaps after restart; reject capability admission if the cache cannot hold the negotiated window. | Both | Pending |
| AC-READ-01 | OpenClaw controller | Read independent retained frames through each controller's local projection; advance only the matching client cursor after background acknowledgement; preserve cursor/gap continuity across reconnect inside the retained window. | Official path | Pending |

## General standalone Surf Ace CLI and Tight Beam consumer skill

Tight Beam also participates in every dual-controller and official-path row that names both controllers.

| AC ID | Primary lane | Required implementation and proof | Proof class | Status |
|---|---|---|---|---|
| AC-ID-01 | Standalone CLI + Tight Beam consumer | Concurrently admit OpenClaw and Tight Beam under distinct stable IDs; let both push and read the same pane; expose no lock, `busy`, designation, claim, takeover, or owner-only refusal. | Official path | Pending |
| AC-ARCH-02 | Standalone CLI + Tight Beam consumer | Prove no coordinator or hard-coded deployment topology participates, the CLI remains caller-neutral Surf Ace infrastructure, Tight Beam remains a separate consumer, and the compositor receives only client-resolved state. | Both | Pending |
| AC-MIG-04 | Standalone CLI + Tight Beam consumer | Exercise `surf-ace` directly from a non-Tight-Beam caller, then the identical executable through an unchanged ordinary Tight Beam archetype and reusable skill, plus OpenClaw's official surface. | Official path | Pending |
| AC-CLI-01 | General standalone CLI | Build and run the same locked Rust source and complete test suite natively on macOS and Linux; canonical schema/conformance vectors and deterministic output fixtures match. | Source/unit | Pending |
| AC-CLI-02 | General standalone CLI | Prove one `surf-ace` executable / `surf-ace-cli` crate and package, exactly eleven commands, no Tight-Beam-specific CLI identity or path, and no MCP/adapter/dedicated-archetype/parallel route. | Source/unit | Pending |
| AC-CLI-03 | Standalone CLI + Tight Beam consumer | Invoke installed `surf-ace` directly without any skill/agent runtime, then prove the Tight Beam skill and fixtures use the exact same executable semantics without changing archetype identity or unrelated material. | Source/unit | Pending |
| AC-CLI-04 | General standalone CLI | Reuse one durable controller ID across sequential processes and serialize concurrent processes over each complete networked invocation. | Source/unit | Pending |
| AC-CLI-05 | General standalone CLI | Prove every specified crash window, exact committed success/failure replay, proven `not_committed`, blocking `still_pending`, duplicate-safe receipt replay, and deterministic `intent_committed → materializing → terminal` target work recovery. | Source/unit | Pending |
| AC-CLI-06 | General standalone CLI | Instrument zero Surf Ace network access for locked local-only `read`, including atomic projected consumption and acknowledgement intent. | Source/unit | Pending |
| AC-CLI-07 | General standalone CLI | Reconcile ordered projection/outbox state and all unresolved receipt IDs; apply response events durably before ack/outbox cleanup; clear client receipts only after accepted ack. | Source/unit | Pending |
| AC-CLI-08 | General standalone CLI | Prove deterministic JSON for every networked command, exact receipt correlation, count/byte capacity, admission after ack, reclamation unavailability, zero target materialization on precommit refusal, committed-intent response before callback, separately correlated append-only target result, and external provenance. | Both | Pending |
| AC-CLI-09 | Corrected-source review | Independently adjudicate the complete CORR-09 authority and invariant set against corrected source and native platform evidence before any tablet-a installation. | Source/unit | Pending |
| AC-CLI-10 | Official-path proof | Through the reversible tablet-a test path, prove unchanged ordinary-archetype mutation, correlated receipt, exact `OpenClaw — Clawline`, preserved OpenClaw, and no competing client/service/persistence path. | Official path | Pending |

## Migration, persistence, and rollback

For T1778, AC-MIG-01/02/05 and AC-OPS-01 include the rollout plan's preserved-
state cutover invariant, source-only Gate 2A, rollback custody in Gate 5, and
official installed-product Gate 6A. Temporary copied-state tests never promote
official-path or soak evidence. Official proof uses the actual Gate 5 provider-a and
portrait-display production state, exact merged artifacts, existing lifecycles, versioned
`surf-ace-runtime-state.json` continuity/transaction state, and the installed
CLI before the R7 clock. Gate 2A individually induces all five deterministic
crash boundaries. Official Gate 6 first completes the genuine-legacy
`surf_ace_list`/`surf_ace_read` compatibility consumption across the complete
durably recorded pane inventory, then invokes the sole
`surf_ace_prepare_migration_now({ fingerprint })` creation action. Gate 6A
proves one observable prepared-before-send gateway lifecycle restart, the same
preparation receipt before and after that restart, and one uninterrupted actual
production cutover, list, and capture; it does not claim individual official
induction of the four narrow post-send/already-lockless cases. Any participating
identity change outside the one recorded interruption or Gate 5 reset
invalidates the complete cutover proof.

### T1778 Gate 6 repair traceability

These G6 rows refine existing T1778/T1770 acceptance; they do not add canonical
acceptance IDs or change Apple/client wire authority.

| Decision ID | Source contract | Implementation boundary | Required evidence |
|---|---|---|---|
| G6-01 | `DESIGN.md` §14.3 `surf_ace_list`, `surf_ace_read`, and `surf_ace_prepare_migration_now`; rollout Gate 6 step 2 | Each legacy read atomically persists returned-material consumption plus the pane post-read digest before response. Preparation requires a complete boundary for the exact current sorted pane/lineage inventory; missing, partial, or stale boundaries return `migration_read_incomplete` without a transaction. Runtime-owner forwarding/restart preserves the marker; topology or pane-lineage change invalidates it. | Source tests prove read ordering, partial/missing/stale negatives, forwarding/restart persistence, inventory invalidation, and no implicit prepare. Official unchanged-portrait-display responses and state hashes bind every pane and the final complete boundary digest. |
| G6-02 | `DESIGN.md` §14.3 `surf_ace_prepare_migration_now`; rollout explicit-post-read invariant and Gate 6 step 3 | The ordinary legacy-state persistence seam records `legacySourceRequirement` only when retained provider-local source exists; compare-by-digest clearing or Gate 5 reset removes it. One separate explicit mutation seam binds the complete read-boundary digest, atomically freezes remaining source/material, and persists `prepared` before success. List/read/start/discovery/pair never create a preparation transaction. | Gate 2A source-requirement lifecycle, no-implicit-transaction, error atomicity, and prepared-read refusal tests; official action output equals the durable transaction and final read-boundary hashes. |
| G6-03 | Preserved-state cutover invariant; AC-MIG-01/02 | Prepared source excludes every pre-boundary item consumed by G6-01. Exact pair lookup is four-state: non-complete `prepared(record)` replays migration; terminal `complete_no_migration(record)` retains audit state but uses a fresh ordinary pair ID, existing resume state/cursors, and no migration; `required_unprepared` alone returns `migration_not_prepared`; `no_legacy_source` admits an ordinary clean lockless pair. Pairing never captures source. | Copied-state every-class/post-read tests; all four pair outcomes; clean and completed Electron/iPhone/iPad admission/reconnect; no duplicate migration receipt/material or source-requirement recreation; official receipt/source inventory, completed ordinary pair, and uninterrupted cutover receipt. |
| G6-04 | `surf_ace_prepare_migration_now` idempotency/errors; rollout Gates 2A, 5, 6, and 6A; AC-MIG-02/05 and AC-OPS-01 | Restart or forward redeploy of the same amended package retains one non-complete recovery receipt/ID. At `complete`, tool retry returns that immutable receipt but ordinary pairs use fresh normal IDs and no migration. Before preparation, captured pre-amendment package rollback is legal; afterward rollback preflight returns `rollback_requires_full_reset` before mutation. Only byte-exact two-product baseline reset, forward redeploy, and identity binding permit a new migration ID. | Same action result before/after gateway restart and immutable complete tool retry; completed Electron/iPhone/iPad reconnect tests; rollback-preflight tests in both states; deterministic crash windows; production rollback refusal followed by provider-a/portrait-display full reset, exact baseline, receipt absence, redeploy, and new-ID evidence. |
| G6-05 | Rollout Gates 1, 3, 4, 6-11 | Independent source-document review precedes implementation; distinct exact combined source review precedes MSR/deploy; final R7/R8 clocks restart from zero with passive extension and shipped Rust CLI only. | Review artifacts and hashes, merged-main/artifact identities, fleet deploy proof, CLI-only R7/R8 logs, rollback custody, Tracker proof rows. |

| AC ID | Primary lane | Required implementation and proof | Proof class | Status |
|---|---|---|---|---|
| AC-MIG-01 | Migration/persistence | Keep the named genuine non-capable portrait-display client at retained source `4a8294446ee8fbfd56ec4cbe7c68218b43fb8836`, archived runtime/state, endpoint `19001`, and existing compositor lifecycle in legacy single-provider mode. After Gate 5 rollback capture and corrected extension deployment, prove unchanged official legacy list/read atomically consumes each returned item and durably completes the exact pane inventory. An early explicit action returns `migration_read_incomplete` without a transaction; only the final complete boundary permits preparation. Never classify a capable endpoint as legacy because migration or lockless admission failed. Clean upgraded Electron/iPhone/iPad clients have `no_legacy_source` and pair normally. | Official path | Pending |
| AC-MIG-02 | Migration/persistence | Convert representative post-read legacy provider-local state to CAP-conforming recoverable client state, bounded scopes/cursors/gaps, bounds, and sequences; never replay a frame/register consumed before the explicit preparation boundary. Admit valid over-`P` state within `P + T`; prove `SURF-12` and projection capacity before admission; reject only actual count/byte/cache violations without trim, drop, clamp, or owner selection. The ordinary legacy-state seam records source-requirement metadata, never a transaction. Only `surf_ace_prepare_migration_now` may persist a fresh source/material snapshot, matching complete `compatibilityReadBoundarySha256`, endpoint/surface, canonical digests, stable pair request ID, and durable `prepared` phase in versioned `surf-ace-runtime-state.json`; list/read/start/discovery/pair may only hydrate or read transaction state. Pair uses the exact four-state lookup and never captures. Non-complete recovery requires exact same-ID `migrationReceiptId`; after `complete`, ordinary Electron/iPhone/iPad reconnect uses a fresh normal pair ID and existing resume state/cursors with no migration material, duplicate receipt, source-requirement recreation, or terminal-state mutation, while tool retry returns the immutable complete receipt. Require client durable commit before response, already-lockless/source-pending recovery, and compare-by-digest clearing. Gate 2A proves every migration class, boundary lifecycle, all four pairing outcomes, clean and completed Electron/iPhone/iPad admission/reconnect, and all five crash boundaries. Gate 6A proves one prepared-before-send `ai.openclaw.gateway` lifecycle restart with unchanged action receipt/transaction/boundary/source/material hashes and same ID, then one uninterrupted actual installed-product cutover, completed ordinary CLI list pairing, and capture through `snapshot.get`; it does not individually induce the remaining narrow windows as official evidence. Start-order reversal alone, ownership clearing, Electron rejection weakening, CLI takeover/retry, harness/direct-wire evidence, proof hooks, or added topology cannot satisfy this row. | Both | Pending |
| AC-MIG-05 | Migration/persistence | Capture provider-a/portrait-display rollback before any official action. Before preparation, captured pre-amendment package rollback is allowed. After a preparation record exists, first prove rollback preflight returns `rollback_requires_full_reset` before changing package bytes, gateway process, or state; an ordinary package rollback may not continue the ID. Then exercise the complete two-product production reset, restoring package, continuity/controller state, retained portrait-display source/artifact/state, lifecycle, and sockets without silently choosing an owner. Retain the first receipt as evidence but prove it absent from baseline; forward-deploy and identity-bind the amended package before creating a fresh ID. Restart or forward redeploy of that same amended package preserves the same ID. Apply the same rule after any failed final interruption/cutover; failed evidence is never reused. | Official path | Pending |
| AC-OPS-01 | Migration/persistence | Restart the production client and restore the complete live/tombstone distribution, CAP state, consumable scopes, controller bundles, limits, sequences, revisions, retained-source requirement, compatibility-read boundary, and retained complete audit transaction before admission; never clamp valid over-`P` state; reconcile controller cache and pending acknowledgement outbox before reporting current. Shared runtime owner forwarding preserves the marker; changed topology or pane lineage invalidates its inventory before preparation. A completed transaction must not block or migrate an ordinary Electron/iPhone/iPad reconnect: use a fresh normal pair ID, restore existing controller state/cursors, and keep the immutable complete receipt available only to audit/tool retry. Prove the OpenClaw lifecycle released/replaced the prior shared runtime and bind old/new gateway PID/start time, installed and loaded module digests, runtime instance, socket ownership, source, process, stable controller identity, and state. Gate 6A may change the gateway PID/start time only through its one recorded prepared-phase `launchctl kickstart -k`; any other participating identity change, or any change after the uninterrupted cutover's passing final binding, invalidates Gate 6A and the complete cutover candidate. | Both | Pending |

## Official-path proof and implementation-review gates

| AC ID | Primary lane | Required implementation and proof | Proof class | Status |
|---|---|---|---|---|
| AC-MIG-03 | Official-path proof | Run the complete applicable check set on every admitted platform with no D1..D5, Q5R1..Q5R5, W1, local-read, structured-overflow, close, or retention carve-out. Permit controller lifecycle omission only where no controller product operation exists; always prove local-user recoverable close. | Official path | Pending |
| AC-PROOF-01 | Official-path proof | Support every user-visible and product claim with official OpenClaw and Tight Beam paths on a production Surf Ace client build; label all harness-only evidence diagnostic. | Official path | Pending — corrected source review must pass before tablet-a proof |
| AC-PROOF-02 | Review gate | Before implementation review passes, land R6-AMD-1..8, R7-AMD-1..2, R8-AMD-1..2, and R9-AMD-1 in `DESIGN.md` and the compositor authority documents, including every specifically named section, and synchronize the canonical shared compositor artifact with its repository projection. | Source/unit | Passed — `DESIGN.md` SHA-256 `5523aa4a5c80912e2a4c99a7dbe6f2d2784c4ea7fee849312e45d92f0ef8c2c2`; canonical and repository compositor projections are byte-identical at SHA-256 `5600c9cb9b250c83330c62b4ab5156bff64c1fce8b4a82bdbdbddc6b04cefa04` |

## Count audit

| Primary lane | Unique acceptance rows |
|---|---:|
| Shared protocol/client-authority core | 43 |
| Electron client/product UI | 6 |
| Apple canonical mapping (crosswalk; no new IDs) | 0 |
| OpenClaw controller | 3 |
| General standalone CLI + Tight Beam consumer skill (including AC-CLI-01..08) | 11 |
| Migration/persistence/rollback | 4 |
| Official-path proof/review gate (including AC-CLI-09..10) | 5 |
| **Total** | **72** |

The 72-row Apple mapping and ten `APPLE-AC` summaries are crosswalks over the same canonical requirements and therefore do not increase the unique 72-row count.
